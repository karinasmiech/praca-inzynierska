# praca-inzynierska

## Run app

* Build backend docker image:

```bash
cd backend
npm run build-image
```

* Build admin docker image:

```bash
cd admnin
npm run build-image
```

* Run docker compose:

```bash
docker-compose up -d
```
